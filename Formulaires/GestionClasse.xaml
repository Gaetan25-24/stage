<UserControl x:Class="MonProjet.GestionMatieres"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.FontSize="13"
             FontFamily="{DynamicResource MaterialDesignFont}"
             xmlns:local="clr-namespace:MonProjet"
             mc:Ignorable="d"  d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="materialDesign:Card">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F5F5F5">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <!-- Header -->
                <Grid Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="BookOpenPageVariant" Width="32" Height="32" 
                                                Foreground="#2196F3" VerticalAlignment="Center"/>
                        <TextBlock Text="Gestion des Classes" FontSize="28" FontWeight="Bold" 
                                  Foreground="#333" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Actions rapides -->
                <materialDesign:Card Style="{StaticResource StatCardStyle}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Actions Rapides" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                        <WrapPanel>
                            <Button Style="{StaticResource MaterialDesignRaisedButton}" 
                                    Background="#4CAF50" Foreground="White" Margin="0,0,10,10" 
                                    Padding="10,0">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Plus" Width="20" Height="20"/>
                                    <TextBlock Text="Ajouter Classe" Margin="10,0,0,0"/>
                                </StackPanel>
                            </Button>
                            <Button Style="{StaticResource MaterialDesignRaisedButton}" 
                                    Background="#2196F3" Foreground="White" Margin="0,0,10,10" 
                                    Padding="10,0">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileExport" Width="20" Height="20"/>
                                    <TextBlock Text="Exporter Liste" Margin="10,0,0,0"/>
                                </StackPanel>
                            </Button>
                        </WrapPanel>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Liste des matières -->
                <materialDesign:Card Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Liste des Classes" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Niveau" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Grid.Column="1" Text="Enseignant Principal" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Grid.Column="2" Text="Actions" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        </Grid>
                        <Separator Margin="0,0,0,10"/>

                        <!-- Entrée matière -->
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="CE2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="M. Dupont" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Style="{StaticResource MaterialDesignIconButton}" 
                                        ToolTip="Modifier">
                                    <materialDesign:PackIcon Kind="Pencil" Foreground="#2196F3"/>
                                </Button>
                                <Button Style="{StaticResource MaterialDesignIconButton}" 
                                        ToolTip="Supprimer">
                                    <materialDesign:PackIcon Kind="Delete" Foreground="#F44336"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                        <Separator Opacity="0.3" Margin="0,5"/>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="CE2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="Mme Martin" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Style="{StaticResource MaterialDesignIconButton}" 
                                        ToolTip="Modifier">
                                    <materialDesign:PackIcon Kind="Pencil" Foreground="#2196F3"/>
                                </Button>
                                <Button Style="{StaticResource MaterialDesignIconButton}" 
                                        ToolTip="Supprimer">
                                    <materialDesign:PackIcon Kind="Delete" Foreground="#F44336"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                        <Separator Opacity="0.3" Margin="0,5"/>

                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="CM2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="M. Bernard" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Style="{StaticResource MaterialDesignIconButton}" 
                                        ToolTip="Modifier">
                                    <materialDesign:PackIcon Kind="Pencil" Foreground="#2196F3"/>
                                </Button>
                                <Button Style="{StaticResource MaterialDesignIconButton}" 
                                        ToolTip="Supprimer">
                                    <materialDesign:PackIcon Kind="Delete" Foreground="#F44336"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
